const names = {
	"a" : "Nitya",
	"b" : "test",
	"c" : "new"
};

let namesArr = Object.values(names);
//console.log(namesArr);

let entriesArr = Object.entries(names);
//console.log(entriesArr);

//console.log(Object.values(names));

let newString = 'loading'.padEnd(100, '.');
console.log(newString);